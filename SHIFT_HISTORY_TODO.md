# Історія Змін - Документація та TODO

## Як це працює зараз

### Архітектура

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Пристрій 1    │     │   Пристрій 2    │     │   Пристрій N    │
│   (браузер)     │     │   (браузер)     │     │   (браузер)     │
└────────┬────────┘     └────────┬────────┘     └────────┬────────┘
         │                       │                       │
         │    POST /activity     │    GET /current       │
         │◄─────────────────────►│◄─────────────────────►│
         │                       │                       │
         └───────────────────────┼───────────────────────┘запуш
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │     Strapi Backend     │
                    │                        │
                    │  ┌──────────────────┐  │
                    │  │  Shift (active)  │  │
                    │  │  - activities[]  │  │
                    │  │  - startedAt     │  │
                    │  │  - status        │  │
                    │  └──────────────────┘  │
                    └────────────────────────┘
```

### API Endpoints

| Endpoint | Метод | Опис |
|----------|-------|------|
| `/api/shifts/current` | GET | Отримати активну зміну (або створити нову) |
| `/api/shifts/current/activity` | POST | Додати активність до поточної зміни |
| `/api/shifts/close` | POST | Закрити поточну зміну |
| `/api/shifts` | GET | Список закритих змін (архів) |
| `/api/shifts/:documentId` | GET | Деталі конкретної зміни |

### Типи активностей

| Тип | Опис | Деталі |
|-----|------|--------|
| `sale` | Продаж | customerName, items[], totalAmount, discount, paymentStatus |
| `writeOff` | Списання | flowerName, length, qty, reason, notes |
| `customerCreate` | Новий клієнт | customerName, phone, email |
| `customerDelete` | Видалення клієнта | customerName |
| `productEdit` | Редагування товару | productName, changes |
| `productCreate` | Створення товару | productName |
| `productDelete` | Видалення товару | productName |
| `paymentConfirm` | Підтвердження оплати | customerName, amount |
| `supply` | Поставка | (поки не реалізовано) |

### Синхронізація

1. **При завантаженні** - `GET /shifts/current` отримує поточну зміну з усіма активностями
2. **При дії** - `POST /shifts/current/activity` додає запис (з optimistic update в UI)
3. **Polling** - кожні 30 сек автоматичне оновлення для синхронізації між пристроями
4. **Ручне оновлення** - кнопка 🔄 в інтерфейсі

---

## TODO: Що ще потрібно зробити

### 🔴 Критичні (потрібно для повноцінної роботи)

#### 1. Додати права доступу до Shift API
```
Файл: backend/src/bootstrap.ts
Завдання: Додати permissions для shift endpoints (як зроблено для інших API)
```

#### 2. Логування поставок (Supply)
```
Файл: frontend/components/ui/planned-supply-modal.tsx
Завдання: При успішній поставці викликати logActivity('supply', {...})
```

#### 3. Логування підтвердження оплати
```
Файл: Потрібно знайти де відбувається confirmPayment
Завдання: При підтвердженні оплати викликати logActivity('paymentConfirm', {...})
```

### 🟡 Важливі покращення

#### 4. Архів закритих змін
```
Файл: frontend/components/sections/history-section.tsx
Завдання:
- Додати вкладку "Архів" з переглядом закритих змін
- GET /api/shifts з пагінацією
- Перегляд деталей старої зміни
```

#### 5. Фільтрація активностей
```
Завдання: Додати фільтри в HistorySection:
- За типом (продажі, списання, тощо)
- За датою/часом
- Пошук за клієнтом/товаром
```

#### 6. Експорт архівних змін
```
Завдання: Можливість експортувати в XLSX будь-яку закриту зміну з архіву
```

### 🟢 Nice to have

#### 7. Нотатки до зміни
```
Завдання: При закритті зміни можливість додати коментар/нотатку
(поле вже є в API - notes)
```

#### 8. Статистика по змінах
```
Завдання: Графіки/діаграми:
- Порівняння виручки по змінах
- Середня тривалість зміни
- Топ товарів за зміну
```

#### 9. WebSocket замість Polling
```
Завдання: Для миттєвої синхронізації замінити polling на WebSocket/SSE
(зараз затримка до 30 сек)
```

#### 10. Редагування товарів в історії
```
Завдання: При редагуванні товару (ціна, залишки) логувати зміни
Файли: Залежить від того, де реалізовано редагування товарів
```

---

## Структура файлів

```
backend/
└── src/api/shift/
    ├── content-types/shift/schema.json  # Схема даних Shift
    ├── controllers/shift.ts              # API логіка
    ├── routes/shift.ts                   # Роутинг
    └── services/shift.ts                 # Сервіси (порожній)

frontend/
├── hooks/
│   └── use-activity-log.ts              # Hook для роботи з активностями
├── components/sections/
│   └── history-section.tsx              # UI компонент історії
├── lib/
│   ├── strapi.ts                        # API функції (getCurrentShift, addShiftActivity, closeShift)
│   └── export.ts                        # Експорт в XLSX (exportShift)
└── app/admin/
    └── admin-client.tsx                 # Інтеграція в адмін панель
```

---

## Приклад додавання нового типу активності

```typescript
// 1. В admin-client.tsx знайти місце де відбувається дія
// 2. Додати виклик logActivity

// Приклад для поставки:
const handleSupplyComplete = async (supplyData) => {
  // ... існуюча логіка ...

  if (result.success) {
    // Логуємо активність
    await logActivity('supply', {
      items: supplyData.items.map(item => ({
        name: item.flowerName,
        length: item.length,
        qty: item.qty,
      })),
      totalItems: supplyData.items.reduce((sum, i) => sum + i.qty, 0),
      supplierName: supplyData.supplier || 'Невідомий',
    });
  }
};
```

---

## Тестування

### Перевірка синхронізації:
1. Відкрити адмін панель на двох пристроях/браузерах
2. На першому зробити продаж
3. На другому натиснути 🔄 або почекати 30 сек
4. Продаж має з'явитися на обох пристроях

### Перевірка закриття зміни:
1. Зробити кілька дій (продаж, списання)
2. Натиснути "Закрити зміну"
3. Перевірити що:
   - Зміна збереглася в Strapi (через Admin Panel)
   - Нова порожня зміна створилася
   - На інших пристроях також порожня історія

---

## Контакти

При питаннях або проблемах - дивитися логи:
- Frontend: DevTools Console
- Backend: Strapi logs (Railway)

/**
 * Clients Section Module
 *
 * Експорт компонентів для секції клієнтів
 */

// Types
export * from './types';

// Main component (re-export from parent for backward compatibility)
export { ClientsSection } from '../clients-section';

# Premium Flora — UX/UI Аудит та План Переробки

**Версія:** 1.0
**Дата:** 2025-12-20
**Автор:** Chief UX/UI Architect & Performance Engineer
**Ціль:** Рівень Apple/Tesla/Stripe — преміум-досвід для B2B оптових клієнтів

---

## 1. Executive Summary

### Ключові проблеми

| # | Проблема | Вплив на конверсію |
|---|----------|-------------------|
| 1 | **Відсутній кошик/чекаут** — тільки "зв'яжіться для замовлення" | Критичний: втрата 60%+ потенційних замовлень |
| 2 | **Немає сторінок**: корпоративні пропозиції, доставка, FAQ, сертифікати | Високий: відсутність довіри для B2B |
| 3 | **Слабка Hero-секція** — відео без чіткого оферу для оптовиків | Високий: перші 5 секунд не конвертують |
| 4 | **Відсутні цифри/соціальний доказ** — немає кейсів, відгуків | Високий: B2B потребує proof points |
| 5 | **Анімації не оптимізовані** — layout thrashing, немає lazy-init | Середній: LCP > 3s на мобільних |
| 6 | **Фільтри каталогу обмежені** — немає MOQ, ціновий діапазон | Середній: friction для оптовиків |
| 7 | **Одне фото продукту** — немає галереї, zoom | Середній: недостатньо для рішення про закупівлю |
| 8 | **Блог в модалці** — погано для SEO, неможливо поділитися | Низький: втрата органічного трафіку |

### Сильні сторони (зберегти)

- Преміальна кольорова гама (emerald/green) — brand identity
- Glassmorphism ефекти — сучасний вигляд
- Responsive grid система — добра база
- Strapi CMS інтеграція — гнучкість контенту
- Dark mode підтримка — повний

### План 30/60/90 днів

#### Days 1-30: Foundation & Quick Wins
- [ ] Sticky CTA "Отримати оптову пропозицію"
- [ ] Hero секція з value proposition
- [ ] Value stacking секція (цифри, SLA)
- [ ] FAQ сторінка
- [ ] Доставка/логістика сторінка
- [ ] Performance: critical CSS, font preload

#### Days 31-60: Conversion Optimization
- [ ] Корпоративні пропозиції сторінка
- [ ] Case studies секція
- [ ] Testimonials з фото партій
- [ ] Сертифікати/довіра сторінка
- [ ] Pricing framing з MOQ
- [ ] Catalog discovery покращення

#### Days 61-90: Advanced Features
- [ ] Кошик + швидке замовлення
- [ ] Bottom-sheet форма для мобільних
- [ ] Page transitions (fade + slide)
- [ ] IntersectionObserver lazy animations
- [ ] Performance metrics: LCP < 2.5s, INP < 200ms

---

## 2. Issue Table

| Issue | Impact | Fix | Effort | Priority |
|-------|--------|-----|--------|----------|
| Немає sticky CTA | Втрата конверсії -40% | Додати sticky footer CTA | S | P0 |
| Hero без оферу | Bounce rate +30% | Переписати Hero з value prop | M | P0 |
| Відсутня FAQ | Support load +50% | Створити /faq сторінку | M | P0 |
| Немає соц. доказу | Trust score -60% | Додати testimonials секцію | M | P1 |
| Анімації без lazy-init | LCP +1.5s | IntersectionObserver | S | P1 |
| Одне фото продукту | Bounce +25% | Додати галерею | L | P1 |
| Немає доставки сторінки | Info gap | Створити /delivery | M | P1 |
| Фільтри без MOQ | Friction +35% | Додати MOQ slider | S | P2 |
| Блог в модалці | SEO -50% | Окремі сторінки | L | P2 |
| Немає корпоративних пропозицій | B2B conversion -45% | Створити /corporate | L | P2 |
| Шрифти без preload | LCP +400ms | Preload woff2 | S | P2 |
| Backdrop-filter везде | FPS drop | Обмежити площу | S | P2 |
| Немає сертифікатів | Trust -30% | Створити /certificates | M | P3 |
| Немає case studies | Social proof -25% | Додати секцію | L | P3 |
| Pricing не прозорий | Friction +20% | Таблиці MOQ/пакети | M | P3 |

**Effort Scale:** S = 1-2 дні, M = 3-5 днів, L = 1-2 тижні
**Priority:** P0 = Critical, P1 = High, P2 = Medium, P3 = Low

---

## 3. Нові Макети Секцій

### 3.1 Hero Section — Premium B2B

```
┌─────────────────────────────────────────────────────────────────┐
│ [Nav: Logo | Каталог | Корпоративним | Доставка | Контакти]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐    ┌─────────────────────────────┐   │
│  │                      │    │ ОПТОВА СВІЖІСТЬ            │   │
│  │     [Hero Video/     │    │ ДО ВАШИХ ДВЕРЕЙ            │   │
│  │      Image]          │    │                            │   │
│  │                      │    │ Прямі поставки з           │   │
│  │                      │    │ європейських плантацій     │   │
│  │                      │    │ для флористів, готелів     │   │
│  │                      │    │ та івент-агентств          │   │
│  └──────────────────────┘    │                            │   │
│                              │ ┌────────────────────────┐ │   │
│                              │ │ Отримати оптову        │ │   │
│                              │ │ пропозицію →           │ │   │
│                              │ └────────────────────────┘ │   │
│                              │                            │   │
│                              │ ┌──────┐ ┌──────┐ ┌──────┐ │   │
│                              │ │ 24h  │ │ 500+ │ │ -20° │ │   │
│                              │ │доставка│ │клієнтів│ │ланцюг│ │   │
│                              │ └──────┘ └──────┘ └──────┘ │   │
│                              └─────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Value Stacking Section

```
┌─────────────────────────────────────────────────────────────────┐
│              ЧОМУ ОБИРАЮТЬ PREMIUM FLORA                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │    ❄️ -20°C   │  │   🚚 24/48h   │  │   📋 ISO     │       │
│  │  Холодний     │  │   Доставка    │  │  Сертифікат  │       │
│  │  ланцюг       │  │   по Україні  │  │  якості      │       │
│  │               │  │               │  │               │       │
│  │  Температурний│  │  Власний      │  │  Всі партії  │       │
│  │  контроль від │  │  рефрижера-   │  │  з докумен-  │       │
│  │  плантації    │  │  торний парк  │  │  тами        │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
│                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │   👤 24/7     │  │   💰 MOQ      │  │   🔄 Гарантія│       │
│  │  Менеджер     │  │   від 50 шт   │  │  Заміни      │       │
│  │               │  │               │  │               │       │
│  │  Персональний │  │  Гнучкі умови │  │  Заміна або  │       │
│  │  менеджер для │  │  для різних   │  │  повернення  │       │
│  │  кожного      │  │  обсягів      │  │  коштів      │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 Catalog Discovery

```
┌─────────────────────────────────────────────────────────────────┐
│ КАТАЛОГ КВІТІВ                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ 🔍 Пошук...  │ Вид ▼ │ Сорт ▼ │ MOQ ▼ │ Ціна ▼ │ [Скинути]││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─ Chips ─────────────────────────────────────────────────────┐│
│ │ [Троянди ×] [50+ шт ×] [До 100 грн ×]                       ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐               │
│ │ [Image] │ │ [Image] │ │ [Image] │ │ [Image] │               │
│ │ Троянда │ │ Троянда │ │ Хризан- │ │ Лілія   │               │
│ │ червона │ │ біла    │ │ тема    │ │ біла    │               │
│ │         │ │         │ │         │ │         │               │
│ │ від 45₴ │ │ від 52₴ │ │ від 35₴ │ │ від 68₴ │               │
│ │ MOQ: 50 │ │ MOQ: 50 │ │ MOQ: 100│ │ MOQ: 25 │               │
│ │ ✓ 250шт │ │ ✓ 180шт │ │ ✓ 500шт │ │ ✓ 75шт  │               │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘               │
│                                                                 │
│               [Показати ще 12 →]                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4 Case Studies Section

```
┌─────────────────────────────────────────────────────────────────┐
│              НАМ ДОВІРЯЮТЬ                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ [Photo: Event setup]                                        ││
│ │                                                             ││
│ │ "Premium Flora забезпечує нас квітами для 50+ весіль       ││
│ │  щомісяця. Жодного зриву за 3 роки співпраці."             ││
│ │                                                             ││
│ │ — Олена Коваленко, Wedding Dreams Agency                   ││
│ │   [Logo]                                                    ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌───────────────────┐ ┌───────────────────┐ ┌─────────────────┐│
│ │ [Logo: Hilton]    │ │ [Logo: Fairmont]  │ │ [Logo: IHG]    ││
│ │ 200+ букетів/міс  │ │ 150+ букетів/міс  │ │ 100+ букетів   ││
│ └───────────────────┘ └───────────────────┘ └─────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ [Grid: 4 photos of flower batches with quality stamps]      ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.5 Logistics Section

```
┌─────────────────────────────────────────────────────────────────┐
│              ЛОГІСТИКА ТА ДОСТАВКА                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────┐ ┌─────────────────────────────┐│
│ │                             │ │ ЗОНИ ДОСТАВКИ              ││
│ │     [Interactive Map]       │ │                             ││
│ │                             │ │ 🟢 Київ — 4-6 годин        ││
│ │     Зона 1: Київ            │ │ 🟡 Область — 12-24 години  ││
│ │     Зона 2: Область         │ │ 🟠 Україна — 24-48 годин   ││
│ │     Зона 3: Україна         │ │                             ││
│ │                             │ │ ─────────────────────────  ││
│ │                             │ │                             ││
│ │                             │ │ ❄️ Холодний ланцюг:        ││
│ │                             │ │    -20°C весь шлях         ││
│ │                             │ │                             ││
│ │                             │ │ 📦 Упаковка:               ││
│ │                             │ │    Термобокси з гелем      ││
│ └─────────────────────────────┘ └─────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ Графік поставок:                                            ││
│ │ ПН | ВТ | СР | ЧТ | ПТ | СБ | НД                            ││
│ │  ✓ |  ✓ |  ✓ |  ✓ |  ✓ |  ○ |  ○                            ││
│ │                                                             ││
│ │ ✓ = Доставка   ○ = Попереднє замовлення до ПТ 15:00        ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.6 Pricing Framing

```
┌─────────────────────────────────────────────────────────────────┐
│              ОПТОВІ ПАКЕТИ                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌───────────────────┐ ┌───────────────────┐ ┌─────────────────┐│
│ │     STARTER       │ │    BUSINESS       │ │   ENTERPRISE   ││
│ │                   │ │   ⭐ Популярний   │ │                 ││
│ ├───────────────────┤ ├───────────────────┤ ├─────────────────┤│
│ │   від 50 шт/міс   │ │  від 500 шт/міс   │ │ від 2000 шт/міс││
│ │                   │ │                   │ │                 ││
│ │ • Базові ціни     │ │ • -10% знижка     │ │ • -20% знижка   ││
│ │ • Email підтримка │ │ • Особистий       │ │ • Виділений     ││
│ │ • Стандартна      │ │   менеджер        │ │   менеджер 24/7 ││
│ │   доставка        │ │ • Пріоритетна     │ │ • Безкоштовна   ││
│ │                   │ │   доставка        │ │   доставка      ││
│ │                   │ │ • Кредитна лінія  │ │ • Відтермін. 30д││
│ │                   │ │                   │ │ • Custom сорти  ││
│ ├───────────────────┤ ├───────────────────┤ ├─────────────────┤│
│ │   [Обрати →]      │ │   [Обрати →]      │ │  [Зв'язатися]  ││
│ └───────────────────┘ └───────────────────┘ └─────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ТАБЛИЦЯ ЦІН (приклад: Троянда червона 50см)                ││
│ ├──────────┬──────────┬──────────┬──────────┬─────────────────┤│
│ │ Кількість│ 50-99    │ 100-499  │ 500-999  │ 1000+           ││
│ ├──────────┼──────────┼──────────┼──────────┼─────────────────┤│
│ │ Ціна/шт  │ 45 ₴     │ 42 ₴     │ 38 ₴     │ Індивідуально   ││
│ └──────────┴──────────┴──────────┴──────────┴─────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.7 Testimonials Section

```
┌─────────────────────────────────────────────────────────────────┐
│              ВІДГУКИ КЛІЄНТІВ                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ← [Slider controls] →                                       ││
│ │                                                             ││
│ │  ┌───────┐                                                  ││
│ │  │[Photo]│  "Працюємо з Premium Flora вже 5 років.         ││
│ │  │       │   Завжди свіжі квіти, завжди вчасно.            ││
│ │  └───────┘   Рекомендую всім флористам Києва."             ││
│ │                                                             ││
│ │             ★★★★★                                           ││
│ │             Марія Петренко                                  ││
│ │             Власниця "Квіткова Майстерня"                   ││
│ │             Клієнт з 2019 року                              ││
│ │                                                             ││
│ │  ● ○ ○ ○ ○  [1/5]                                           ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ СЕРТИФІКАТИ ТА ДОКУМЕНТИ                                    ││
│ │                                                             ││
│ │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            ││
│ │ │[ISO]    │ │[HACCP]  │ │[Фіто]   │ │[Якість] │            ││
│ │ │9001:2015│ │         │ │сертифік.│ │         │            ││
│ └─┴─────────┴─┴─────────┴─┴─────────┴─┴─────────┴────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.8 FAQ Section

```
┌─────────────────────────────────────────────────────────────────┐
│              ЧАСТІ ПИТАННЯ                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ 🔍 Пошук по питаннях...                                     ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─ Категорії ─────────────────────────────────────────────────┐│
│ │ [Замовлення] [Доставка] [Оплата] [Повернення] [Сезонність] ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ▼ Який мінімальний обсяг замовлення?                        ││
│ │   ─────────────────────────────────────────────────────────  ││
│ │   Мінімальне замовлення від 50 одиниць одного виду.         ││
│ │   Для постійних клієнтів можливі індивідуальні умови.       ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ▶ Як відбувається доставка в регіони?                       ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ▶ Чи можна замовити нестандартні сорти?                     ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ ▶ Яка гарантія на свіжість квітів?                          ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ [Не знайшли відповідь? Напишіть нам →]                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.9 Final CTA (Sticky Footer)

```
┌─────────────────────────────────────────────────────────────────┐
│ ╔═══════════════════════════════════════════════════════════╗ │
│ ║  🌸 Готові обговорити оптову співпрацю?                    ║ │
│ ║                                                            ║ │
│ ║  ┌──────────────────────┐  ┌──────────────────────────┐   ║ │
│ ║  │ Отримати пропозицію  │  │  +380 (44) 123-45-67     │   ║ │
│ ║  │        →             │  │       📞                 │   ║ │
│ ║  └──────────────────────┘  └──────────────────────────┘   ║ │
│ ╚═══════════════════════════════════════════════════════════╝ │
└─────────────────────────────────────────────────────────────────┘

Mobile Bottom Sheet variant:
┌─────────────────────────────────────────────────────────────────┐
│ ══════════════════════════════════════════════════════════════ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Ім'я                                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Телефон                                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Компанія (опційно)                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            Отримати пропозицію →                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. Код Прикладів (React/Next.js + Framer Motion)

### 4.1 Встановлення залежностей

```bash
npm install framer-motion @radix-ui/react-accordion @radix-ui/react-dialog
```

### 4.2 Hero Section Component

```tsx
// components/client/hero-section-premium.tsx
'use client';

import { motion, useReducedMotion } from 'framer-motion';
import { ArrowRight, Phone, Snowflake, Truck, Users } from 'lucide-react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';

const stats = [
  { icon: Truck, value: '24h', label: 'Доставка' },
  { icon: Users, value: '500+', label: 'Клієнтів' },
  { icon: Snowflake, value: '-20°C', label: 'Ланцюг' },
];

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.1,
      delayChildren: 0.2,
    },
  },
};

const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      type: 'spring',
      stiffness: 100,
      damping: 15,
    },
  },
};

export function HeroSectionPremium() {
  const shouldReduceMotion = useReducedMotion();

  return (
    <section className="relative min-h-[90vh] flex items-center overflow-hidden">
      {/* Background Video/Image */}
      <div className="absolute inset-0 z-0">
        <video
          autoPlay
          muted
          loop
          playsInline
          poster="/images/hero-poster.webp"
          className="w-full h-full object-cover"
        >
          <source src="/hero-video.mp4" type="video/mp4" />
        </video>
        <div className="absolute inset-0 bg-gradient-to-r from-white/95 via-white/80 to-transparent" />
      </div>

      {/* Content */}
      <div className="container relative z-10 mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          variants={shouldReduceMotion ? {} : containerVariants}
          initial="hidden"
          animate="visible"
          className="max-w-2xl"
        >
          {/* Badge */}
          <motion.div variants={itemVariants}>
            <span className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-emerald-50 text-emerald-700 text-sm font-medium mb-6">
              <Snowflake className="w-4 h-4" />
              Холодний ланцюг від плантації до вас
            </span>
          </motion.div>

          {/* Headline */}
          <motion.h1
            variants={itemVariants}
            className="text-4xl sm:text-5xl lg:text-6xl font-bold text-slate-900 leading-tight mb-6"
          >
            Оптова свіжість
            <br />
            <span className="text-emerald-600">до ваших дверей</span>
          </motion.h1>

          {/* Subheadline */}
          <motion.p
            variants={itemVariants}
            className="text-lg sm:text-xl text-slate-600 mb-8 max-w-xl"
          >
            Прямі поставки з європейських плантацій для флористів,
            готелів та івент-агентств. Гарантована свіжість 7+ днів.
          </motion.p>

          {/* CTAs */}
          <motion.div
            variants={itemVariants}
            className="flex flex-col sm:flex-row gap-4 mb-12"
          >
            <Button size="lg" asChild>
              <Link href="/corporate" className="gap-2">
                Отримати оптову пропозицію
                <ArrowRight className="w-5 h-5" />
              </Link>
            </Button>
            <Button variant="outline" size="lg" asChild>
              <a href="tel:+380441234567" className="gap-2">
                <Phone className="w-5 h-5" />
                +380 (44) 123-45-67
              </a>
            </Button>
          </motion.div>

          {/* Stats */}
          <motion.div
            variants={itemVariants}
            className="flex gap-6 sm:gap-10"
          >
            {stats.map((stat, index) => (
              <motion.div
                key={stat.label}
                className="flex flex-col items-center"
                initial={{ opacity: 0, scale: 0.8 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ delay: 0.5 + index * 0.1 }}
              >
                <div className="w-12 h-12 rounded-xl bg-emerald-50 flex items-center justify-center mb-2">
                  <stat.icon className="w-6 h-6 text-emerald-600" />
                </div>
                <span className="text-2xl font-bold text-slate-900">
                  {stat.value}
                </span>
                <span className="text-sm text-slate-500">{stat.label}</span>
              </motion.div>
            ))}
          </motion.div>
        </motion.div>
      </div>
    </section>
  );
}
```

### 4.3 Value Stacking Section

```tsx
// components/client/value-stacking.tsx
'use client';

import { motion, useInView } from 'framer-motion';
import { useRef } from 'react';
import {
  Snowflake,
  Truck,
  FileCheck,
  UserCheck,
  Coins,
  RefreshCw,
} from 'lucide-react';

const values = [
  {
    icon: Snowflake,
    title: 'Холодний ланцюг -20°C',
    description: 'Температурний контроль від плантації до вашого складу',
    color: 'bg-cyan-50 text-cyan-600',
  },
  {
    icon: Truck,
    title: 'Доставка 24/48h',
    description: 'Власний рефрижераторний парк по всій Україні',
    color: 'bg-emerald-50 text-emerald-600',
  },
  {
    icon: FileCheck,
    title: 'ISO Сертифікат',
    description: 'Всі партії з повною документацією якості',
    color: 'bg-violet-50 text-violet-600',
  },
  {
    icon: UserCheck,
    title: 'Менеджер 24/7',
    description: 'Персональний менеджер для кожного клієнта',
    color: 'bg-amber-50 text-amber-600',
  },
  {
    icon: Coins,
    title: 'MOQ від 50 шт',
    description: 'Гнучкі умови для різних обсягів замовлень',
    color: 'bg-rose-50 text-rose-600',
  },
  {
    icon: RefreshCw,
    title: 'Гарантія заміни',
    description: 'Заміна або повернення коштів без питань',
    color: 'bg-blue-50 text-blue-600',
  },
];

export function ValueStackingSection() {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: '-100px' });

  return (
    <section ref={ref} className="py-20 sm:py-28 bg-slate-50/50">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl sm:text-4xl font-bold text-slate-900 mb-4">
            Чому обирають Premium Flora
          </h2>
          <p className="text-lg text-slate-600 max-w-2xl mx-auto">
            Ми поєднуємо європейську якість з українським сервісом
          </p>
        </motion.div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {values.map((value, index) => (
            <motion.div
              key={value.title}
              initial={{ opacity: 0, y: 30 }}
              animate={isInView ? { opacity: 1, y: 0 } : {}}
              transition={{
                duration: 0.5,
                delay: index * 0.1,
                type: 'spring',
                stiffness: 100,
              }}
            >
              <div className="group relative bg-white rounded-2xl p-6 shadow-sm border border-slate-100 hover:shadow-lg hover:border-emerald-100 transition-all duration-300 hover:-translate-y-1">
                <div
                  className={`w-14 h-14 rounded-xl ${value.color} flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300`}
                >
                  <value.icon className="w-7 h-7" />
                </div>
                <h3 className="text-lg font-semibold text-slate-900 mb-2">
                  {value.title}
                </h3>
                <p className="text-slate-600">{value.description}</p>
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

### 4.4 Sticky CTA Component

```tsx
// components/client/sticky-cta.tsx
'use client';

import { motion, AnimatePresence, useScroll } from 'framer-motion';
import { useState, useEffect } from 'react';
import { ArrowRight, Phone, X } from 'lucide-react';
import { Button } from '@/components/ui/button';

export function StickyCTA() {
  const [isVisible, setIsVisible] = useState(false);
  const [isDismissed, setIsDismissed] = useState(false);
  const { scrollY } = useScroll();

  useEffect(() => {
    const unsubscribe = scrollY.on('change', (latest) => {
      // Show after scrolling 500px
      setIsVisible(latest > 500 && !isDismissed);
    });
    return () => unsubscribe();
  }, [scrollY, isDismissed]);

  return (
    <AnimatePresence>
      {isVisible && (
        <motion.div
          initial={{ y: 100, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          exit={{ y: 100, opacity: 0 }}
          transition={{ type: 'spring', stiffness: 300, damping: 30 }}
          className="fixed bottom-0 left-0 right-0 z-50 p-4 sm:p-0"
        >
          <div className="mx-auto max-w-4xl">
            <div className="relative bg-white/95 backdrop-blur-lg rounded-2xl sm:rounded-b-none shadow-2xl border border-slate-200 p-4 sm:p-6">
              <button
                onClick={() => setIsDismissed(true)}
                className="absolute top-3 right-3 p-1 text-slate-400 hover:text-slate-600 transition-colors"
                aria-label="Закрити"
              >
                <X className="w-5 h-5" />
              </button>

              <div className="flex flex-col sm:flex-row items-center justify-between gap-4">
                <div className="text-center sm:text-left">
                  <p className="text-lg font-semibold text-slate-900">
                    Готові обговорити оптову співпрацю?
                  </p>
                  <p className="text-sm text-slate-600">
                    Отримайте персональну пропозицію за 24 години
                  </p>
                </div>

                <div className="flex gap-3">
                  <Button variant="outline" size="lg" asChild>
                    <a href="tel:+380441234567" className="gap-2">
                      <Phone className="w-5 h-5" />
                      <span className="hidden sm:inline">Зателефонувати</span>
                    </a>
                  </Button>
                  <Button size="lg" className="gap-2">
                    Отримати пропозицію
                    <ArrowRight className="w-5 h-5" />
                  </Button>
                </div>
              </div>
            </div>
          </div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

### 4.5 FAQ Accordion Component

```tsx
// components/client/faq-section.tsx
'use client';

import { motion, useInView } from 'framer-motion';
import { useRef, useState } from 'react';
import * as Accordion from '@radix-ui/react-accordion';
import { ChevronDown, Search } from 'lucide-react';
import { cn } from '@/lib/utils';

const categories = [
  'Всі',
  'Замовлення',
  'Доставка',
  'Оплата',
  'Повернення',
  'Сезонність',
];

const faqs = [
  {
    category: 'Замовлення',
    question: 'Який мінімальний обсяг замовлення?',
    answer:
      'Мінімальне замовлення від 50 одиниць одного виду. Для постійних клієнтів можливі індивідуальні умови та зниження MOQ.',
  },
  {
    category: 'Доставка',
    question: 'Як відбувається доставка в регіони?',
    answer:
      'Доставка здійснюється власним рефрижераторним транспортом. Київ — 4-6 годин, область — 12-24 години, вся Україна — 24-48 годин. Температурний режим -20°C підтримується весь шлях.',
  },
  {
    category: 'Замовлення',
    question: 'Чи можна замовити нестандартні сорти?',
    answer:
      'Так, ми працюємо з індивідуальними замовленнями. Зв\'яжіться з менеджером, і ми підберемо потрібні сорти з наших партнерських плантацій в Нідерландах, Еквадорі та Кенії.',
  },
  {
    category: 'Повернення',
    question: 'Яка гарантія на свіжість квітів?',
    answer:
      'Гарантуємо свіжість 7+ днів з моменту доставки при правильному зберіганні. Якщо якість не відповідає стандартам — безкоштовна заміна або повернення коштів.',
  },
  {
    category: 'Оплата',
    question: 'Які способи оплати доступні?',
    answer:
      'Приймаємо безготівковий розрахунок (з ПДВ та без), оплату картою, готівку при доставці. Для постійних клієнтів доступна кредитна лінія до 30 днів.',
  },
  {
    category: 'Сезонність',
    question: 'Як змінюються ціни в сезон свят?',
    answer:
      'Ціни можуть коригуватися в період високого попиту (14 лютого, 8 березня). Рекомендуємо бронювати заздалегідь — за 2-3 тижні до свята для фіксації ціни.',
  },
];

export function FAQSection() {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: '-100px' });
  const [activeCategory, setActiveCategory] = useState('Всі');
  const [searchQuery, setSearchQuery] = useState('');

  const filteredFaqs = faqs.filter((faq) => {
    const matchesCategory =
      activeCategory === 'Всі' || faq.category === activeCategory;
    const matchesSearch =
      faq.question.toLowerCase().includes(searchQuery.toLowerCase()) ||
      faq.answer.toLowerCase().includes(searchQuery.toLowerCase());
    return matchesCategory && matchesSearch;
  });

  return (
    <section ref={ref} className="py-20 sm:py-28">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-4xl">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.5 }}
          className="text-center mb-12"
        >
          <h2 className="text-3xl sm:text-4xl font-bold text-slate-900 mb-4">
            Часті питання
          </h2>
          <p className="text-lg text-slate-600">
            Відповіді на популярні питання про співпрацю
          </p>
        </motion.div>

        {/* Search */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.5, delay: 0.1 }}
          className="relative mb-6"
        >
          <Search className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" />
          <input
            type="text"
            placeholder="Пошук по питаннях..."
            value={searchQuery}
            onChange={(e) => setSearchQuery(e.target.value)}
            className="w-full pl-12 pr-4 py-3 rounded-xl border border-slate-200 bg-white focus:border-emerald-300 focus:ring-2 focus:ring-emerald-200/50 transition-all"
          />
        </motion.div>

        {/* Categories */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.5, delay: 0.2 }}
          className="flex flex-wrap gap-2 mb-8"
        >
          {categories.map((category) => (
            <button
              key={category}
              onClick={() => setActiveCategory(category)}
              className={cn(
                'px-4 py-2 rounded-full text-sm font-medium transition-all',
                activeCategory === category
                  ? 'bg-emerald-600 text-white shadow-lg shadow-emerald-500/25'
                  : 'bg-slate-100 text-slate-600 hover:bg-slate-200'
              )}
            >
              {category}
            </button>
          ))}
        </motion.div>

        {/* Accordion */}
        <Accordion.Root type="single" collapsible className="space-y-3">
          {filteredFaqs.map((faq, index) => (
            <motion.div
              key={faq.question}
              initial={{ opacity: 0, y: 20 }}
              animate={isInView ? { opacity: 1, y: 0 } : {}}
              transition={{ duration: 0.5, delay: 0.3 + index * 0.05 }}
            >
              <Accordion.Item
                value={faq.question}
                className="bg-white rounded-xl border border-slate-200 overflow-hidden data-[state=open]:shadow-lg data-[state=open]:border-emerald-200 transition-all"
              >
                <Accordion.Trigger className="flex items-center justify-between w-full p-5 text-left font-semibold text-slate-900 hover:text-emerald-600 transition-colors group">
                  <span>{faq.question}</span>
                  <ChevronDown className="w-5 h-5 text-slate-400 group-data-[state=open]:rotate-180 transition-transform duration-300" />
                </Accordion.Trigger>
                <Accordion.Content className="overflow-hidden data-[state=open]:animate-accordion-down data-[state=closed]:animate-accordion-up">
                  <div className="px-5 pb-5 text-slate-600 leading-relaxed">
                    {faq.answer}
                  </div>
                </Accordion.Content>
              </Accordion.Item>
            </motion.div>
          ))}
        </Accordion.Root>

        {filteredFaqs.length === 0 && (
          <p className="text-center text-slate-500 py-8">
            Питань не знайдено. Спробуйте інший пошук.
          </p>
        )}

        {/* Contact CTA */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={isInView ? { opacity: 1 } : {}}
          transition={{ duration: 0.5, delay: 0.5 }}
          className="text-center mt-12"
        >
          <p className="text-slate-600 mb-4">Не знайшли відповідь?</p>
          <a
            href="/contacts"
            className="inline-flex items-center gap-2 text-emerald-600 font-semibold hover:text-emerald-700 transition-colors"
          >
            Напишіть нам →
          </a>
        </motion.div>
      </div>
    </section>
  );
}
```

### 4.6 Product Card with Micro-animations

```tsx
// components/client/product-card-premium.tsx
'use client';

import { motion } from 'framer-motion';
import Image from 'next/image';
import Link from 'next/link';
import { Package, Check } from 'lucide-react';
import { cn } from '@/lib/utils';

interface ProductCardPremiumProps {
  product: {
    id: string;
    slug: string;
    name: string;
    image?: string;
    minPrice: number;
    moq: number;
    stock: number;
    variants: Array<{ length: number; price: number }>;
  };
  index?: number;
}

export function ProductCardPremium({
  product,
  index = 0,
}: ProductCardPremiumProps) {
  const inStock = product.stock > 0;

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{
        duration: 0.4,
        delay: index * 0.05,
        type: 'spring',
        stiffness: 100,
      }}
      whileHover={{ y: -4 }}
      className="group"
    >
      <Link href={`/catalog/${product.slug}`} className="block">
        <article className="relative bg-white rounded-2xl border border-slate-100 overflow-hidden shadow-sm hover:shadow-xl hover:border-emerald-100 transition-all duration-300">
          {/* Image Container */}
          <div className="relative aspect-square overflow-hidden bg-slate-50">
            {product.image ? (
              <Image
                src={product.image}
                alt={product.name}
                fill
                sizes="(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 25vw"
                className="object-cover transition-transform duration-500 group-hover:scale-110"
              />
            ) : (
              <div className="absolute inset-0 flex items-center justify-center text-slate-300">
                <Package className="w-16 h-16" />
              </div>
            )}

            {/* Stock Badge */}
            <div
              className={cn(
                'absolute top-3 right-3 px-3 py-1 rounded-full text-xs font-medium backdrop-blur-sm',
                inStock
                  ? 'bg-emerald-500/90 text-white'
                  : 'bg-slate-500/90 text-white'
              )}
            >
              {inStock ? (
                <span className="flex items-center gap-1">
                  <Check className="w-3 h-3" />
                  {product.stock} шт
                </span>
              ) : (
                'Під замовлення'
              )}
            </div>

            {/* Quick Info Overlay */}
            <motion.div
              initial={{ opacity: 0 }}
              whileHover={{ opacity: 1 }}
              className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent flex items-end p-4"
            >
              <div className="text-white text-sm">
                <p className="font-medium">Розміри:</p>
                <p>
                  {product.variants.map((v) => `${v.length}см`).join(', ')}
                </p>
              </div>
            </motion.div>
          </div>

          {/* Content */}
          <div className="p-4">
            <h3 className="font-semibold text-slate-900 mb-2 group-hover:text-emerald-600 transition-colors">
              {product.name}
            </h3>

            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm text-slate-500">від</p>
                <p className="text-xl font-bold text-emerald-600">
                  {product.minPrice} ₴
                </p>
              </div>

              <div className="text-right">
                <p className="text-xs text-slate-500">MOQ</p>
                <p className="text-sm font-medium text-slate-700">
                  {product.moq} шт
                </p>
              </div>
            </div>
          </div>

          {/* Hover Border Effect */}
          <motion.div
            className="absolute inset-0 rounded-2xl border-2 border-emerald-400 pointer-events-none"
            initial={{ opacity: 0 }}
            whileHover={{ opacity: 1 }}
            transition={{ duration: 0.2 }}
          />
        </article>
      </Link>
    </motion.div>
  );
}
```

### 4.7 Bottom Sheet for Mobile Quick Order

```tsx
// components/client/quick-order-sheet.tsx
'use client';

import { useState, useEffect } from 'react';
import { motion, AnimatePresence, useDragControls } from 'framer-motion';
import { X, Send, Phone, Building2 } from 'lucide-react';
import { Button } from '@/components/ui/button';

interface QuickOrderSheetProps {
  isOpen: boolean;
  onClose: () => void;
}

export function QuickOrderSheet({ isOpen, onClose }: QuickOrderSheetProps) {
  const dragControls = useDragControls();
  const [formData, setFormData] = useState({
    name: '',
    phone: '',
    company: '',
  });
  const [isSubmitting, setIsSubmitting] = useState(false);

  // Lock body scroll when open
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = '';
    }
    return () => {
      document.body.style.overflow = '';
    };
  }, [isOpen]);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setIsSubmitting(true);
    // Simulate API call
    await new Promise((resolve) => setTimeout(resolve, 1000));
    setIsSubmitting(false);
    onClose();
    // Show success toast
  };

  return (
    <AnimatePresence>
      {isOpen && (
        <>
          {/* Backdrop */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            onClick={onClose}
            className="fixed inset-0 z-50 bg-black/40 backdrop-blur-sm"
          />

          {/* Sheet */}
          <motion.div
            initial={{ y: '100%' }}
            animate={{ y: 0 }}
            exit={{ y: '100%' }}
            transition={{ type: 'spring', damping: 30, stiffness: 300 }}
            drag="y"
            dragControls={dragControls}
            dragConstraints={{ top: 0, bottom: 0 }}
            dragElastic={{ top: 0, bottom: 0.5 }}
            onDragEnd={(_, info) => {
              if (info.offset.y > 100) onClose();
            }}
            className="fixed bottom-0 left-0 right-0 z-50 bg-white rounded-t-3xl shadow-2xl max-h-[90vh] overflow-hidden"
          >
            {/* Drag Handle */}
            <div
              className="flex justify-center pt-3 pb-2 cursor-grab active:cursor-grabbing"
              onPointerDown={(e) => dragControls.start(e)}
            >
              <div className="w-10 h-1 rounded-full bg-slate-300" />
            </div>

            {/* Header */}
            <div className="flex items-center justify-between px-6 pb-4 border-b border-slate-100">
              <h2 className="text-xl font-bold text-slate-900">
                Швидке замовлення
              </h2>
              <button
                onClick={onClose}
                className="p-2 rounded-full hover:bg-slate-100 transition-colors"
              >
                <X className="w-5 h-5 text-slate-500" />
              </button>
            </div>

            {/* Form */}
            <form onSubmit={handleSubmit} className="p-6 space-y-4">
              <div>
                <label className="block text-sm font-medium text-slate-700 mb-1.5">
                  Ваше ім'я *
                </label>
                <input
                  type="text"
                  required
                  value={formData.name}
                  onChange={(e) =>
                    setFormData({ ...formData, name: e.target.value })
                  }
                  className="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-300 focus:ring-2 focus:ring-emerald-200/50 transition-all"
                  placeholder="Олена Коваленко"
                />
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-700 mb-1.5">
                  Телефон *
                </label>
                <div className="relative">
                  <Phone className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" />
                  <input
                    type="tel"
                    required
                    value={formData.phone}
                    onChange={(e) =>
                      setFormData({ ...formData, phone: e.target.value })
                    }
                    className="w-full pl-12 pr-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-300 focus:ring-2 focus:ring-emerald-200/50 transition-all"
                    placeholder="+380 (XX) XXX-XX-XX"
                  />
                </div>
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-700 mb-1.5">
                  Компанія (опційно)
                </label>
                <div className="relative">
                  <Building2 className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" />
                  <input
                    type="text"
                    value={formData.company}
                    onChange={(e) =>
                      setFormData({ ...formData, company: e.target.value })
                    }
                    className="w-full pl-12 pr-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-300 focus:ring-2 focus:ring-emerald-200/50 transition-all"
                    placeholder="Назва компанії"
                  />
                </div>
              </div>

              <Button
                type="submit"
                size="lg"
                className="w-full mt-6"
                disabled={isSubmitting}
              >
                {isSubmitting ? (
                  <span className="flex items-center gap-2">
                    <motion.div
                      animate={{ rotate: 360 }}
                      transition={{ repeat: Infinity, duration: 1 }}
                      className="w-5 h-5 border-2 border-white border-t-transparent rounded-full"
                    />
                    Надсилаємо...
                  </span>
                ) : (
                  <span className="flex items-center gap-2">
                    <Send className="w-5 h-5" />
                    Отримати пропозицію
                  </span>
                )}
              </Button>

              <p className="text-xs text-slate-500 text-center mt-4">
                Натискаючи кнопку, ви погоджуєтесь з{' '}
                <a href="/privacy" className="text-emerald-600 hover:underline">
                  політикою конфіденційності
                </a>
              </p>
            </form>

            {/* Safe area spacer for iOS */}
            <div className="h-6 sm:h-0" />
          </motion.div>
        </>
      )}
    </AnimatePresence>
  );
}
```

### 4.8 Page Transition Wrapper

```tsx
// components/layout/page-transition.tsx
'use client';

import { motion, AnimatePresence } from 'framer-motion';
import { usePathname } from 'next/navigation';
import { ReactNode } from 'react';

interface PageTransitionProps {
  children: ReactNode;
}

const pageVariants = {
  initial: {
    opacity: 0,
    y: 8,
  },
  animate: {
    opacity: 1,
    y: 0,
  },
  exit: {
    opacity: 0,
    y: -8,
  },
};

const pageTransition = {
  type: 'tween',
  ease: [0.25, 0.1, 0.25, 1],
  duration: 0.3,
};

export function PageTransition({ children }: PageTransitionProps) {
  const pathname = usePathname();

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={pathname}
        initial="initial"
        animate="animate"
        exit="exit"
        variants={pageVariants}
        transition={pageTransition}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  );
}
```

### 4.9 Intersection Observer Hook for Lazy Animations

```tsx
// hooks/use-scroll-animation.ts
'use client';

import { useEffect, useRef, useState } from 'react';

interface UseScrollAnimationOptions {
  threshold?: number;
  rootMargin?: string;
  triggerOnce?: boolean;
}

export function useScrollAnimation({
  threshold = 0.1,
  rootMargin = '-50px',
  triggerOnce = true,
}: UseScrollAnimationOptions = {}) {
  const ref = useRef<HTMLElement>(null);
  const [isVisible, setIsVisible] = useState(false);
  const [hasTriggered, setHasTriggered] = useState(false);

  useEffect(() => {
    const element = ref.current;
    if (!element) return;

    // Check for reduced motion preference
    const prefersReducedMotion = window.matchMedia(
      '(prefers-reduced-motion: reduce)'
    ).matches;

    if (prefersReducedMotion) {
      setIsVisible(true);
      return;
    }

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsVisible(true);
          if (triggerOnce) {
            setHasTriggered(true);
            observer.unobserve(element);
          }
        } else if (!triggerOnce && !hasTriggered) {
          setIsVisible(false);
        }
      },
      { threshold, rootMargin }
    );

    observer.observe(element);

    return () => observer.disconnect();
  }, [threshold, rootMargin, triggerOnce, hasTriggered]);

  return { ref, isVisible };
}

// Usage example:
// const { ref, isVisible } = useScrollAnimation();
// <motion.div ref={ref} animate={isVisible ? 'visible' : 'hidden'} />
```

---

## 5. CSS Приклади

### 5.1 Fluid Typography System

```css
/* Add to globals.css */

/* ===== PREMIUM FLUID TYPOGRAPHY ===== */
:root {
  /* Fluid type scale - Mobile first */
  --fs-xs: clamp(0.6875rem, 0.625rem + 0.2vw, 0.75rem);
  --fs-sm: clamp(0.8125rem, 0.75rem + 0.2vw, 0.875rem);
  --fs-base: clamp(0.9375rem, 0.875rem + 0.2vw, 1rem);
  --fs-lg: clamp(1.0625rem, 1rem + 0.2vw, 1.125rem);
  --fs-xl: clamp(1.1875rem, 1.1rem + 0.3vw, 1.25rem);
  --fs-2xl: clamp(1.375rem, 1.25rem + 0.4vw, 1.5rem);
  --fs-3xl: clamp(1.625rem, 1.4rem + 0.7vw, 1.875rem);
  --fs-4xl: clamp(1.875rem, 1.6rem + 0.9vw, 2.25rem);
  --fs-5xl: clamp(2.25rem, 1.8rem + 1.4vw, 3rem);
  --fs-6xl: clamp(2.75rem, 2rem + 2.2vw, 3.75rem);
  --fs-7xl: clamp(3.25rem, 2.2rem + 3.2vw, 4.5rem);

  /* Display headings - Hero/Landing */
  --fs-display: clamp(2.5rem, 1.5rem + 4vw, 5rem);
  --fs-display-sm: clamp(2rem, 1.2rem + 3vw, 3.5rem);

  /* Line heights */
  --lh-tight: 1.1;
  --lh-snug: 1.25;
  --lh-normal: 1.5;
  --lh-relaxed: 1.625;
  --lh-loose: 2;

  /* Letter spacing */
  --ls-tighter: -0.05em;
  --ls-tight: -0.025em;
  --ls-normal: 0;
  --ls-wide: 0.025em;
  --ls-wider: 0.05em;
  --ls-widest: 0.1em;
}

/* Heading utilities */
.heading-display {
  font-size: var(--fs-display);
  line-height: var(--lh-tight);
  letter-spacing: var(--ls-tight);
  font-weight: 700;
}

.heading-1 {
  font-size: var(--fs-5xl);
  line-height: var(--lh-tight);
  letter-spacing: var(--ls-tight);
  font-weight: 700;
}

.heading-2 {
  font-size: var(--fs-4xl);
  line-height: var(--lh-snug);
  letter-spacing: var(--ls-tight);
  font-weight: 700;
}

.heading-3 {
  font-size: var(--fs-3xl);
  line-height: var(--lh-snug);
  font-weight: 600;
}

.heading-4 {
  font-size: var(--fs-2xl);
  line-height: var(--lh-snug);
  font-weight: 600;
}

/* Body text utilities */
.body-lg {
  font-size: var(--fs-lg);
  line-height: var(--lh-relaxed);
}

.body-base {
  font-size: var(--fs-base);
  line-height: var(--lh-relaxed);
}

.body-sm {
  font-size: var(--fs-sm);
  line-height: var(--lh-normal);
}

.caption {
  font-size: var(--fs-xs);
  line-height: var(--lh-normal);
  letter-spacing: var(--ls-wide);
}

.overline {
  font-size: var(--fs-xs);
  line-height: var(--lh-normal);
  letter-spacing: var(--ls-widest);
  text-transform: uppercase;
  font-weight: 600;
}
```

### 5.2 Reduced Motion & Accessibility

```css
/* ===== ACCESSIBILITY ===== */

/* Reduced motion - respect user preferences */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  /* Disable parallax and complex transforms */
  .parallax-element,
  .hover-lift-3d,
  .animate-float {
    transform: none !important;
  }

  /* Keep opacity transitions for fade effects */
  .fade-in-up,
  .fade-in {
    opacity: 1 !important;
    transform: none !important;
  }
}

/* High contrast mode */
@media (prefers-contrast: high) {
  :root {
    --border-color: currentColor;
    --shadow-sm: none;
    --shadow-md: none;
    --shadow-lg: none;
  }

  .glass,
  .glass-soft,
  .glass-strong {
    background: var(--surface);
    backdrop-filter: none;
  }

  button,
  a {
    text-decoration: underline;
  }
}

/* Focus styles - visible for keyboard navigation */
:focus-visible {
  outline: 3px solid var(--brand-primary);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

/* Remove default focus for mouse users */
:focus:not(:focus-visible) {
  outline: none;
}

/* Skip to main content link */
.skip-link {
  position: absolute;
  top: -100%;
  left: 50%;
  transform: translateX(-50%);
  z-index: 9999;
  padding: 1rem 2rem;
  background: var(--brand-primary);
  color: white;
  border-radius: var(--radius-md);
  text-decoration: none;
  font-weight: 600;
  transition: top 0.2s;
}

.skip-link:focus {
  top: 1rem;
}

/* Screen reader only */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
```

### 5.3 Performance-Optimized Animations

```css
/* ===== 120FPS ANIMATIONS ===== */

/* GPU-accelerated base */
.gpu-accelerated {
  will-change: transform, opacity;
  transform: translateZ(0);
  backface-visibility: hidden;
}

/* Optimized keyframes - only transform/opacity */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translate3d(0, 20px, 0);
  }
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0);
  }
}

@keyframes fadeInScale {
  from {
    opacity: 0;
    transform: scale3d(0.95, 0.95, 1);
  }
  to {
    opacity: 1;
    transform: scale3d(1, 1, 1);
  }
}

@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translate3d(30px, 0, 0);
  }
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0);
  }
}

@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translate3d(-30px, 0, 0);
  }
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0);
  }
}

/* Staggered reveal - CSS only */
.stagger-reveal > * {
  opacity: 0;
  transform: translate3d(0, 20px, 0);
  animation: fadeInUp 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.stagger-reveal > *:nth-child(1) { animation-delay: 0ms; }
.stagger-reveal > *:nth-child(2) { animation-delay: 50ms; }
.stagger-reveal > *:nth-child(3) { animation-delay: 100ms; }
.stagger-reveal > *:nth-child(4) { animation-delay: 150ms; }
.stagger-reveal > *:nth-child(5) { animation-delay: 200ms; }
.stagger-reveal > *:nth-child(6) { animation-delay: 250ms; }
.stagger-reveal > *:nth-child(7) { animation-delay: 300ms; }
.stagger-reveal > *:nth-child(8) { animation-delay: 350ms; }
.stagger-reveal > *:nth-child(n+9) { animation-delay: 400ms; }

/* Micro-interactions */
.hover-lift {
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.hover-lift:hover {
  transform: translate3d(0, -4px, 0);
}

.hover-scale {
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.hover-scale:hover {
  transform: scale3d(1.02, 1.02, 1);
}

.hover-scale:active {
  transform: scale3d(0.98, 0.98, 1);
}

/* Button press effect */
.btn-press {
  transition: transform 0.1s ease-out;
}

.btn-press:active {
  transform: scale3d(0.96, 0.96, 1);
}

/* Filter chip animation */
.filter-chip {
  transition:
    transform 0.15s cubic-bezier(0.34, 1.56, 0.64, 1),
    background-color 0.15s ease,
    box-shadow 0.15s ease;
}

.filter-chip:hover {
  transform: translate3d(0, -2px, 0);
}

.filter-chip.active {
  transform: scale3d(1.05, 1.05, 1);
}

/* Skeleton loading - optimized */
@keyframes shimmer {
  0% {
    transform: translateX(-100%);
  }
  100% {
    transform: translateX(100%);
  }
}

.skeleton {
  position: relative;
  overflow: hidden;
  background: linear-gradient(
    90deg,
    var(--skeleton-base) 0%,
    var(--skeleton-shine) 50%,
    var(--skeleton-base) 100%
  );
  background-size: 200% 100%;
}

.skeleton::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.4),
    transparent
  );
  animation: shimmer 1.5s infinite;
  will-change: transform;
}

/* Optimized backdrop-filter */
.glass-optimized {
  /* Limit blur area for performance */
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);

  /* Reduce saturation calculation */
  /* backdrop-filter: blur(8px) saturate(120%); - removed for perf */
}

@supports not (backdrop-filter: blur(8px)) {
  .glass-optimized {
    background: rgba(255, 255, 255, 0.95);
  }
}
```

### 5.4 Touch-Friendly Mobile Styles

```css
/* ===== MOBILE-FIRST TOUCH OPTIMIZATION ===== */

/* Touch targets - 48px minimum */
@media (pointer: coarse) {
  button,
  a,
  [role="button"],
  input[type="checkbox"],
  input[type="radio"],
  select {
    min-height: 48px;
    min-width: 48px;
  }

  /* Increase spacing for touch */
  .touch-spacing {
    gap: 12px;
  }

  /* Disable hover effects on touch */
  .hover-lift:hover,
  .hover-scale:hover {
    transform: none;
  }

  /* Add active states instead */
  .hover-lift:active {
    transform: scale3d(0.98, 0.98, 1);
  }
}

/* Safe area insets for notched devices */
.safe-area-inset {
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
  padding-bottom: env(safe-area-inset-bottom);
}

/* Sticky CTA on mobile */
.sticky-cta-mobile {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 50;
  padding: 1rem;
  padding-bottom: calc(1rem + env(safe-area-inset-bottom));
  background: linear-gradient(
    to top,
    rgba(255, 255, 255, 1) 60%,
    rgba(255, 255, 255, 0)
  );
}

/* Bottom sheet styles */
.bottom-sheet {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 100;
  max-height: 90vh;
  max-height: 90dvh; /* Dynamic viewport height */
  border-radius: 24px 24px 0 0;
  box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.15);
  overscroll-behavior: contain;
}

/* Hide scrollbar on mobile carousel */
.mobile-carousel {
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.mobile-carousel::-webkit-scrollbar {
  display: none;
}

/* Snap scrolling for product carousel */
.snap-carousel {
  scroll-snap-type: x mandatory;
  scroll-padding: 1rem;
}

.snap-carousel > * {
  scroll-snap-align: start;
}
```

### 5.5 Critical CSS for Hero (inline in head)

```css
/* Critical CSS - inline in <head> for LCP optimization */
/* Size: ~2KB minified */

*,*::before,*::after{box-sizing:border-box;margin:0}
html{-webkit-text-size-adjust:100%;line-height:1.5}
body{font-family:var(--font-geist-sans),system-ui,-apple-system,sans-serif;background:#f8fbf9;color:#0f172a}

/* Above-the-fold hero */
.hero{position:relative;min-height:90vh;display:flex;align-items:center;overflow:hidden}
.hero-bg{position:absolute;inset:0;z-index:0}
.hero-bg video{width:100%;height:100%;object-fit:cover}
.hero-overlay{position:absolute;inset:0;background:linear-gradient(to right,rgba(255,255,255,.95),rgba(255,255,255,.8),transparent)}
.hero-content{position:relative;z-index:10;max-width:42rem;padding:0 1rem}

/* Hero typography */
.hero h1{font-size:clamp(2rem,5vw,3.75rem);font-weight:700;line-height:1.1;color:#0f172a}
.hero h1 span{color:#0f9c6e}
.hero p{font-size:clamp(1rem,2vw,1.25rem);color:#475569;margin-top:1.5rem}

/* Primary button */
.btn-primary{display:inline-flex;align-items:center;gap:.5rem;padding:.875rem 1.5rem;background:#0f9c6e;color:#fff;font-weight:600;border-radius:1rem;text-decoration:none;transition:background .2s}
.btn-primary:hover{background:#0d8a5f}

/* Navigation */
.nav{position:fixed;top:0;left:0;right:0;z-index:50;padding:1rem;background:rgba(255,255,255,.8);backdrop-filter:blur(8px)}
.nav-inner{max-width:80rem;margin:0 auto;display:flex;align-items:center;justify-content:space-between}

/* Stats pills */
.stats{display:flex;gap:1.5rem;margin-top:2rem}
.stat{display:flex;flex-direction:column;align-items:center}
.stat-icon{width:3rem;height:3rem;background:#e6f4ef;border-radius:.75rem;display:flex;align-items:center;justify-content:center}
.stat-value{font-size:1.5rem;font-weight:700;color:#0f172a;margin-top:.5rem}
.stat-label{font-size:.875rem;color:#64748b}
```

---

## 6. Quick Wins (10 змін за 48 годин)

### QW-1: Sticky CTA Footer (2 години)
```tsx
// Додати в layout.tsx
import { StickyCTA } from '@/components/client/sticky-cta';

export default function RootLayout({ children }) {
  return (
    <html lang="uk">
      <body>
        {children}
        <StickyCTA />
      </body>
    </html>
  );
}
```

### QW-2: Hero Value Proposition (3 години)
Замінити поточний Hero на версію з чітким офером для B2B. Див. компонент `hero-section-premium.tsx` вище.

### QW-3: Preload Fonts (30 хвилин)
```tsx
// app/layout.tsx
import { Geist, Geist_Mono } from 'next/font/google';

const geist = Geist({
  subsets: ['latin', 'cyrillic'],
  display: 'swap',
  preload: true,
  variable: '--font-geist-sans',
});
```

### QW-4: Critical CSS Inline (1 година)
```tsx
// app/layout.tsx
export default function RootLayout({ children }) {
  return (
    <html lang="uk">
      <head>
        <style dangerouslySetInnerHTML={{ __html: `
          /* Critical CSS тут - див. секцію 5.5 */
        `}} />
      </head>
      <body>{children}</body>
    </html>
  );
}
```

### QW-5: Image Optimization (2 години)
```tsx
// next.config.ts
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
    imageSizes: [16, 32, 48, 64, 96, 128, 256],
    minimumCacheTTL: 31536000,
  },
};
```

### QW-6: Skip Link для Accessibility (30 хвилин)
```tsx
// components/layout/skip-link.tsx
export function SkipLink() {
  return (
    <a href="#main-content" className="skip-link">
      Перейти до основного контенту
    </a>
  );
}
```

### QW-7: MOQ Badge на Product Cards (1 година)
```tsx
// Додати в product-card.tsx
<div className="text-right">
  <p className="text-xs text-slate-500">MOQ</p>
  <p className="text-sm font-medium text-slate-700">
    {product.moq || 50} шт
  </p>
</div>
```

### QW-8: Phone CTA в Navigation (30 хвилин)
```tsx
// Додати в navigation.tsx
<a
  href="tel:+380441234567"
  className="hidden sm:flex items-center gap-2 text-emerald-600 font-semibold"
>
  <Phone className="w-4 h-4" />
  +380 (44) 123-45-67
</a>
```

### QW-9: Lazy Animation Init (1 година)
Замінити прямі анімації на IntersectionObserver-based. Див. `use-scroll-animation.ts` вище.

### QW-10: Breadcrumbs на Product Page (1 година)
```tsx
// app/catalog/[id]/page.tsx
<nav className="mb-6 text-sm" aria-label="Breadcrumb">
  <ol className="flex items-center gap-2 text-slate-500">
    <li><Link href="/" className="hover:text-emerald-600">Головна</Link></li>
    <li>/</li>
    <li><Link href="/catalog" className="hover:text-emerald-600">Каталог</Link></li>
    <li>/</li>
    <li className="text-slate-900 font-medium">{product.name}</li>
  </ol>
</nav>
```

---

## 7. Диф-пакет для CMS (Strapi)

### Нові Content Types

```json
// backend/src/api/faq/content-types/faq/schema.json
{
  "kind": "collectionType",
  "collectionName": "faqs",
  "info": {
    "singularName": "faq",
    "pluralName": "faqs",
    "displayName": "FAQ"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "question": {
      "type": "string",
      "required": true
    },
    "answer": {
      "type": "richtext",
      "required": true
    },
    "category": {
      "type": "enumeration",
      "enum": ["ordering", "delivery", "payment", "returns", "seasonality"],
      "required": true
    },
    "order": {
      "type": "integer",
      "default": 0
    },
    "slug": {
      "type": "uid",
      "targetField": "question"
    }
  }
}
```

```json
// backend/src/api/testimonial/content-types/testimonial/schema.json
{
  "kind": "collectionType",
  "collectionName": "testimonials",
  "info": {
    "singularName": "testimonial",
    "pluralName": "testimonials",
    "displayName": "Testimonial"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "author": {
      "type": "string",
      "required": true
    },
    "company": {
      "type": "string"
    },
    "role": {
      "type": "string"
    },
    "content": {
      "type": "text",
      "required": true
    },
    "rating": {
      "type": "integer",
      "min": 1,
      "max": 5,
      "default": 5
    },
    "photo": {
      "type": "media",
      "allowedTypes": ["images"]
    },
    "companyLogo": {
      "type": "media",
      "allowedTypes": ["images"]
    },
    "clientSince": {
      "type": "date"
    },
    "featured": {
      "type": "boolean",
      "default": false
    },
    "slug": {
      "type": "uid",
      "targetField": "author"
    }
  }
}
```

```json
// backend/src/api/case-study/content-types/case-study/schema.json
{
  "kind": "collectionType",
  "collectionName": "case_studies",
  "info": {
    "singularName": "case-study",
    "pluralName": "case-studies",
    "displayName": "Case Study"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "client": {
      "type": "string",
      "required": true
    },
    "industry": {
      "type": "enumeration",
      "enum": ["hotel", "event", "retail", "corporate", "other"]
    },
    "challenge": {
      "type": "richtext"
    },
    "solution": {
      "type": "richtext"
    },
    "results": {
      "type": "richtext"
    },
    "metrics": {
      "type": "json"
    },
    "heroImage": {
      "type": "media",
      "allowedTypes": ["images"]
    },
    "gallery": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"]
    },
    "clientLogo": {
      "type": "media",
      "allowedTypes": ["images"]
    },
    "testimonialQuote": {
      "type": "text"
    },
    "testimonialAuthor": {
      "type": "string"
    },
    "featured": {
      "type": "boolean",
      "default": false
    },
    "slug": {
      "type": "uid",
      "targetField": "title"
    }
  }
}
```

### Оновлення Flower Schema

```json
// Додати до backend/src/api/flower/content-types/flower/schema.json
{
  "attributes": {
    // ... existing attributes ...
    "moq": {
      "type": "integer",
      "default": 50,
      "min": 1
    },
    "seoTitle": {
      "type": "string",
      "maxLength": 60
    },
    "seoDescription": {
      "type": "text",
      "maxLength": 160
    },
    "gallery": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"]
    },
    "featured": {
      "type": "boolean",
      "default": false
    },
    "origin": {
      "type": "enumeration",
      "enum": ["netherlands", "ecuador", "kenya", "colombia", "ukraine"]
    },
    "seasonality": {
      "type": "enumeration",
      "enum": ["year-round", "spring", "summer", "autumn", "winter"]
    }
  }
}
```

### Single Types для Статичних Сторінок

```json
// backend/src/api/page-delivery/content-types/page-delivery/schema.json
{
  "kind": "singleType",
  "collectionName": "page_deliveries",
  "info": {
    "singularName": "page-delivery",
    "pluralName": "page-deliveries",
    "displayName": "Page: Delivery"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "heroTitle": {
      "type": "string",
      "required": true,
      "default": "Логістика та доставка"
    },
    "heroDescription": {
      "type": "text"
    },
    "zones": {
      "type": "json"
    },
    "schedule": {
      "type": "json"
    },
    "coldChainInfo": {
      "type": "richtext"
    },
    "seoTitle": {
      "type": "string"
    },
    "seoDescription": {
      "type": "text"
    }
  }
}
```

### API Endpoints для Frontend

```typescript
// frontend/lib/strapi.ts - додати нові функції

export async function getFAQs(category?: string) {
  const filters = category ? `&filters[category][$eq]=${category}` : '';
  const res = await fetch(
    `${STRAPI_URL}/api/faqs?sort=order:asc&populate=*${filters}`
  );
  return res.json();
}

export async function getTestimonials(featured?: boolean) {
  const filters = featured ? '&filters[featured][$eq]=true' : '';
  const res = await fetch(
    `${STRAPI_URL}/api/testimonials?populate=*${filters}`
  );
  return res.json();
}

export async function getCaseStudies(featured?: boolean) {
  const filters = featured ? '&filters[featured][$eq]=true' : '';
  const res = await fetch(
    `${STRAPI_URL}/api/case-studies?populate=*${filters}`
  );
  return res.json();
}

export async function getDeliveryPage() {
  const res = await fetch(
    `${STRAPI_URL}/api/page-delivery?populate=*`
  );
  return res.json();
}
```

---

## 8. Rollback Інструкції

### Загальна Стратегія Rollback

```bash
# Створити backup branch перед змінами
git checkout -b backup/pre-ux-audit-$(date +%Y%m%d)
git push origin backup/pre-ux-audit-$(date +%Y%m%d)

# Для rollback будь-якої зміни:
git revert <commit-hash>
# або
git checkout backup/pre-ux-audit-YYYYMMDD -- <file-path>
```

### Rollback за Компонентами

#### Hero Section
```bash
# Повернути оригінальний Hero
git checkout HEAD~1 -- frontend/components/client/hero-section.tsx
# або видалити новий і відновити старий
rm frontend/components/client/hero-section-premium.tsx
git checkout origin/main -- frontend/components/client/hero-section.tsx
```

#### Sticky CTA
```bash
# Видалити з layout
git checkout HEAD~1 -- frontend/app/layout.tsx
# Видалити компонент
rm frontend/components/client/sticky-cta.tsx
```

#### CSS Зміни
```bash
# Повернути globals.css
git checkout origin/main -- frontend/app/globals.css
# Або конкретну секцію - знайти по коментарю
git diff HEAD~1 -- frontend/app/globals.css | grep -A 20 "PREMIUM FLUID"
```

#### Strapi Content Types
```bash
# Видалити нові API
rm -rf backend/src/api/faq
rm -rf backend/src/api/testimonial
rm -rf backend/src/api/case-study
rm -rf backend/src/api/page-delivery

# Перезапустити Strapi для rebuild
cd backend && yarn develop
```

#### Залежності
```bash
# Видалити нові пакети
cd frontend
npm uninstall framer-motion @radix-ui/react-accordion
# Відновити package.json
git checkout origin/main -- package.json package-lock.json
npm install
```

### Database Rollback (Strapi)

```bash
# Backup поточної бази
cp backend/.tmp/data.db backend/.tmp/data.db.backup-$(date +%Y%m%d)

# Rollback до попередньої версії
cp backend/.tmp/data.db.backup-YYYYMMDD backend/.tmp/data.db

# Або очистити та rebuild
rm backend/.tmp/data.db
cd backend && yarn develop
# Дані будуть re-seeded з bootstrap.ts
```

### Моніторинг після Rollback

```bash
# Перевірити build
cd frontend && npm run build

# Перевірити TypeScript
npm run type-check

# Перевірити лінтер
npm run lint

# Smoke test
npm run dev
# Відкрити http://localhost:3000 і перевірити всі сторінки
```

---

## Додаток A: JSON-LD Structured Data

```tsx
// components/seo/product-jsonld.tsx
export function ProductJSONLD({ product }: { product: Product }) {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "Product",
    "name": product.name,
    "description": product.description,
    "image": product.image,
    "sku": product.slug,
    "brand": {
      "@type": "Brand",
      "name": "Premium Flora"
    },
    "offers": {
      "@type": "AggregateOffer",
      "priceCurrency": "UAH",
      "lowPrice": product.minPrice,
      "highPrice": product.maxPrice,
      "offerCount": product.variants.length,
      "availability": product.stock > 0
        ? "https://schema.org/InStock"
        : "https://schema.org/PreOrder"
    }
  };

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
    />
  );
}
```

```tsx
// components/seo/organization-jsonld.tsx
export function OrganizationJSONLD() {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "Premium Flora",
    "url": "https://premiumflora.ua",
    "logo": "https://premiumflora.ua/logo.png",
    "description": "Оптова торгівля свіжими квітами для флористів, готелів та івент-агентств",
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Київ",
      "addressCountry": "UA"
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "telephone": "+380-44-123-45-67",
      "contactType": "sales",
      "availableLanguage": ["Ukrainian", "English"]
    },
    "sameAs": [
      "https://www.instagram.com/premiumflora",
      "https://www.facebook.com/premiumflora"
    ]
  };

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
    />
  );
}
```

---

## Додаток B: Analytics Events

```typescript
// lib/analytics.ts
export const trackEvent = (
  eventName: string,
  eventParams?: Record<string, unknown>
) => {
  // Google Analytics 4
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', eventName, eventParams);
  }

  // Facebook Pixel
  if (typeof window !== 'undefined' && window.fbq) {
    window.fbq('track', eventName, eventParams);
  }
};

// Event names
export const EVENTS = {
  // CTA Events
  CTA_CLICK: 'cta_click',
  CTA_STICKY_CLICK: 'cta_sticky_click',
  CTA_HERO_CLICK: 'cta_hero_click',

  // Form Events
  FORM_START: 'form_start',
  FORM_SUBMIT: 'form_submit',
  FORM_ERROR: 'form_error',

  // Catalog Events
  PRODUCT_VIEW: 'view_item',
  PRODUCT_LIST_VIEW: 'view_item_list',
  FILTER_APPLY: 'filter_apply',
  SEARCH: 'search',

  // Engagement Events
  FAQ_EXPAND: 'faq_expand',
  TESTIMONIAL_VIEW: 'testimonial_view',
  CASE_STUDY_VIEW: 'case_study_view',

  // Phone Events
  PHONE_CLICK: 'phone_click',
  TELEGRAM_CLICK: 'telegram_click',
};

// Usage example
// trackEvent(EVENTS.CTA_CLICK, { location: 'hero', button_text: 'Отримати пропозицію' });
```

---

**Кінець документа**

*Цей аудит підготовлено для Premium Flora як комплексний план переробки UX/UI для досягнення преміум-рівня конверсії B2B клієнтів.*

# ===========================================
# LOCAL DEVELOPMENT
# ===========================================

# Database (SQLite for local dev)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Server
HOST=0.0.0.0
PORT=1337

# Security Keys (generate with: node scripts/generate-secrets.js)
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=your-api-token-salt
ADMIN_JWT_SECRET=your-admin-jwt-secret
JWT_SECRET=your-jwt-secret
TRANSFER_TOKEN_SALT=your-transfer-token-salt
ENCRYPTION_KEY=your-encryption-key

# ===========================================
# PRODUCTION (Railway) - Copy these to Railway Variables
# ===========================================

# NODE_ENV=production
# PROXY=true

# Database (PostgreSQL on Railway)
# DATABASE_CLIENT=postgres
# DATABASE_URL=postgresql://postgres:password@host:port/railway
# DATABASE_SSL=true
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Server URLs (replace with your Railway domain)
# SERVER_URL=https://your-app.up.railway.app
# PUBLIC_URL=https://your-app.up.railway.app

# Frontend URL for CORS (replace with your Vercel domain)
# FRONTEND_URL=https://your-app.vercel.app

# DigitalOcean Spaces (use Spaces Access Keys, NOT Personal Tokens!)
# DO_SPACE_KEY=your-spaces-access-key
# DO_SPACE_SECRET=your-spaces-secret-key
# DO_SPACE_BUCKET=your-bucket-name
# DO_SPACE_ENDPOINT=https://fra1.digitaloceanspaces.com
# DO_SPACE_REGION=fra1

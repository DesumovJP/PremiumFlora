[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "yarn start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Healthcheck disabled - Railway will not check service health
# [healthcheck]
# path = "/api/health"
# timeout = 100
# interval = 300
